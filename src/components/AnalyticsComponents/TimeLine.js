import React from "react";
import { TimeLineContainer } from "../../styles/AnalyticsPage"

const TimeLine = ({session, steps}) => {
  return (
    <TimeLineContainer pad="large" direction="row" gap="medium">
      timeline
    </TimeLineContainer>
  )
};

export default TimeLine;